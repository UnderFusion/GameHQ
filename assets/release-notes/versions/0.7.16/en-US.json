{
  "$schema": "../../../../i18n/schema/release-note-document.schema.json",
  "schema_version": 2,
  "version": "0.7.16",
  "locale": "en-US",
  "date": "2026-09-09",
  "sections": [
    {
      "id": "fixed",
      "title": "Fixed",
      "items": [
        {
          "id": "fixed-sound-load-status",
          "text": "Interface sounds no longer report themselves as loaded before Windows has finished loading them. Each sound now records when it becomes playable, or why it could not be used."
        },
        {
          "id": "fixed-sound-load-warning",
          "text": "If any interface sound is unavailable, GameHQ shows a single warning instead of staying silent without explanation. The new warning text uses English until the planned translation pass."
        }
      ]
    }
  ]
}
